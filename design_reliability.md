# 確定/概算データ判別の仕組み — 設計方針と実装記録

作成日: 2026-09-14 / 対象: js/data.js, js/app.js, css/style.css
ステータス: **実装済み**（承認内容：3段階 / 案1 / 案A）

> 実装中に判明して設計から変えた点は §8「実装時の変更点」にまとめてある。
> 特に **復路（帰りのルート）の作り方** は、当初の想定になかった対応が必要になった。

---

## 0. 調査でわかったこと（前提の訂正を含む）

### 0-1. lookupLocalTrain() が呼ばれていない「本当の理由」
デッドコードなのはご指摘のとおりですが、原因は実装漏れではなく **入口（引数）が
公開版UIから消えている** ことでした。

```js
function lookupLocalTrain(departureText, shinkansenStation, dateStr)
//                        ^^^^^^^^^^^^^ 自由入力の「出発地テキスト」が必要
```

- 個人版には「出発地」自由入力欄があり、そこに入力した地名を
  DEPARTURE_TO_LOCAL_STATION が最寄り駅にマップし、在来線区間を挿入する設計だった
- 公開版は app.js:472 のコメントどおり **「公開版：departure欄は削除済み」** で、
  今は 都道府県プルダウン → 新幹線駅プルダウン の2段選択のみ
- PREFECTURE_STATIONS（js/data.js:1）には在来線駅が無い（新幹線駅しか載っていない）

→ したがって「元の形のまま復活」は物理的に不可能です。引数の設計から変える必要があります。

### 0-2. 公開版では新幹線の発車時刻そのものが「架空」
generateShinkansenTimeline（js/data.js:1622）にこう書かれています。

```js
// 仮想ダイヤ
const yamabikoSchedule = generateHourlySchedule([12]); // 毎時12分
const hayabusaSchedule = generateHourlySchedule([53]); // 毎時53分
```

さらに SHINKANSEN_SCHEDULES / SCHEDULE_ROUTES は公開版で中身が空（js/data.js:1047, 1056）。
個人の出発地特定を避けるため実ダイヤを削除した経緯があり、
lookupTripSchedule() は常に null を返し、必ず概算計算にフォールバックします。

飛行機側の flightSchedule も `['08:30','10:15',...]` のようなハードコード配列で、
実在の便ではありません。

→ **「確定 / 概算」の2値にすると、しおりの交通行がほぼ全部「概算」になり、
バッジが情報として機能しません。** ちゃんと調べた「知床エアポートライナー136分
（斜里バス確認）」と、アプリが勝手に作った「毎時12分発」が同じ扱いになってしまいます。

### 0-3. しおり画面には、そもそも交通タイムラインが描画されていない
app.js:1231 / 1324 を見ると、しおりの1日目・3日目はこうなっています。

```js
day1Events.push({ type: 'transfer', title: '行きのルート（右の経路図を参照）', icon: '🚄' });
```

生成した往復タイムライン（generateShinkansenTimeline / generateFlightTimeline の結果）は
**入力画面の比較カード（destination-info）にしか出ておらず、しおりには一切出ていません。**
しおりに載っているのは、ユーザーがアップした乗換案内のスクショ画像だけです。

→ 「しおり画面でフラグを見せる」には、バッジを付ける前に
**しおりに交通行程を描画するかどうか**を決める必要があります（§4で選択肢を提示）。

### 0-4. しおり上で現在「概算」なのに区別がついていないもの

| 箇所 | 実態 | 現状の表示 |
|---|---|---|
| 現地移動（タクシー）所要 | estimateMovement(): 2地点の「駅からの所要分」の差 + 5分 | 「🚕 タクシー等で移動 約25分」— 断定的 |
| 現地移動 運賃 | estimateTaxiFare(): 所要分 × 0.5km で距離を逆算（js/data.js:1194 に既知の誤差メモあり） | 「¥3,200」— 円単位で断定的 |
| 交通費合計 | **固定 ¥60,000**（東京-函館想定） | 注記はあるが金額は他項目と同じ見た目 |
| 滞在の目安 | 種別ごとの固定値（食事60分等） | 「滞在の目安：約60分」 |

---

## 1. 統一データモデル: reliability（3段階）

boolean の isApproximateSchedule を捨て、新幹線側・飛行機側・現地移動で共通に使える
**1つのメタデータ構造**に置き換えます。

```js
// js/data.js 冒頭付近に新設
const RELIABILITY = {
  VERIFIED:   'verified',    // 公式時刻表・公示運賃を直接確認。source と verifiedDate が必ずある
  RESEARCHED: 'researched',  // 複数ソースで一致を確認した実測値の代表値。実在するが「この日のこの便」ではない
  ESTIMATED:  'estimated',   // アプリが機械生成した値（仮想ダイヤ・距離換算・固定値）
};

// 値に付ける共通の形
function reliability(level, { source = null, verifiedDate = null, note = null, caveat = null } = {}) {
  return { level, source, verifiedDate, note, caveat };
}
```

3段階にする理由は §0-2 のとおり。現状の公開版を2段階で塗ると9割が「概算」になり、
「ちゃんと調べた値」と「アプリの当て推量」の差が消えます。

### 各データの割り当て（実データに基づく現状マッピング）

| データ | level | 根拠 |
|---|---|---|
| CITY_STATION_CONNECTIONS.hakodate_liner の時刻・運賃 | VERIFIED | 駅探 / 七飯町公式 / JR北海道公式・2026-08-06 |
| LOCAL_TRAIN_CONNECTIONS の平日ダイヤ（区間を登録した場合） | VERIFIED | JR公式時刻表・確認日を添える |
| 同 土休日（schedule.holiday が null のとき平日で代用） | ESTIMATED | 未確認ダイヤの流用。**現 isApproximateSchedule の移行先** |
| SHINKANSEN_FARES（宇都宮 / 那須塩原 - 函館） | VERIFIED | Yahoo!路線情報・駅探 2サイト一致・2026-08-04 |
| HOKKAIDO_TAXI_FARE_ZONES | VERIFIED | 北海道運輸局公示第61号・2026-09-13 |
| 空港→目的地のローカル交通（知床136分、根室200分 等） | RESEARCHED | 各コメントの「要検証日2026-09-13」を構造化 |
| 新幹線の発車時刻（generateHourlySchedule） | ESTIMATED | 仮想ダイヤ |
| フライトの便（flightSchedule 配列） | ESTIMATED | ハードコード |
| getTravelTimes() の「概算:」コメント付き区間 | ESTIMATED | コメントどおり |
| getTravelTimes() の「公式確認済み」区間 | RESEARCHED | 所要時間のみ確認済（便は架空） |
| estimateMovement / estimateTaxiFare | ESTIMATED | 距離換算 |
| 交通費 固定 ¥60,000 | ESTIMATED | 固定値 |

---

## 2. 飛行機側: if文の連鎖を「表」に外出しする

現状 generateFlightTimeline（js/data.js:1849-1963）は、所要時間・表示文言・出典コメントが
if/else の中に散らばっています。ここが「構造化フラグを一切持てない」原因です。

```js
// Before（現状）
} else if (destName.includes('知床')) {
  // 女満別空港→ウトロ温泉バスターミナル：知床エアポートライナーで約136分…
  // （斜里バス系列の運行情報で確認、要検証日2026-09-13）。
  // ⚠ この直行バスは冬季・夏季の季節限定運行で、通年運行ではない点は未反映。
  destAirport = '女満別空港';
  localTransfer = 136;
  localTransText = '🚌 知床エアポートライナー等（約136分）';
}
```

```js
// After（提案）— js/data.js に新設するテーブル
const AIRPORT_LOCAL_TRANSIT = {
  '知床': {
    airport: '女満別空港',
    durationMin: 136,
    label: '🚌 知床エアポートライナー等',
    reliability: reliability(RELIABILITY.RESEARCHED, {
      source: '斜里バス系列 運行情報',
      verifiedDate: '2026-09-13',
      caveat: '冬季・夏季の季節限定運行。通年運行ではない点は未反映',
    }),
  },
  '根室': {
    airport: 'たんちょう釧路空港',
    durationMin: 200,
    label: '🚌＋🚃 連絡バス・JR等',
    reliability: reliability(RELIABILITY.RESEARCHED, {
      source: '駅探・バス比較なび',
      verifiedDate: '2026-09-13',
      caveat: '乗り継ぎ待ち次第で176〜269分と幅がある',
    }),
  },
  // …江差 / 旭川 / 富良野 / 美瑛 / 網走 / 釧路 / 摩周湖 / 帯広 / 稚内 /
  //   千歳 / 苫小牧 / 小樽 / ニセコ / 洞爺湖 / 登別 / 積丹
};
```

- コメントに書いてあった情報がそのままフィールドになるので、**コメントとコードの二重管理が消える**
- 出典・確認日が無い項目（網走30分・釧路45分・帯広40分・稚内30分・旭川40分 など、
  コメントすら無いもの）は素直に ESTIMATED として登録する ← 現状は他と見分けがつかない
- 出発地→空港側（airportTransferTime / airportTransText）も同じ形で AIRPORT_ACCESS テーブルに外出し
- caveat は「季節運行」「乗り継ぎで幅がある」等の注意書き。バッジのツールチップと行程下の注記に出す

**この作業で、既存コメントを読み落として値を取りこぼすのが一番怖いので、
テーブル化後に「移行前後で全目的地の localTransfer / airportTransferTime が
1分も変わっていないこと」を確認するスクリプトを流します。**（挙動は一切変えない）

---

## 3. 在来線側（lookupLocalTrain）をどうするか

### 案1 ★推奨: 引数を「駅名」に変え、CITY_STATION_CONNECTIONS と同じ方式に揃える

```js
// Before: lookupLocalTrain(departureText, shinkansenStation, dateStr)
//         ↑ 消えた自由入力欄が前提
// After:  lookupLocalTrain(localStationName, shinkansenStation, dateStr)
//         ↑ プルダウンで選ばれた駅名をそのまま渡せる
```

- DEPARTURE_TO_LOCAL_STATION（キーワード曖昧マッチ）を廃止し、
  LOCAL_TRAIN_CONNECTIONS の `'在来線駅-新幹線駅'` キーだけで引く
  → CITY_STATION_CONNECTIONS（hakodate_liner）と同じ「駅ペアキー」方式に統一
- isApproximateSchedule は `reliability` に置き換え
  （土休日ダイヤ未確認時 → ESTIMATED + note: '土休日ダイヤ未確認のため平日ダイヤを代用'）
- **公開版では在来線駅が駅リストに無いので実際には発火しません。**
  ただし関数は「呼べば動く」状態で生き、PREFECTURE_STATIONS に在来線駅を1つ足せば
  即座に機能します。個人版とのデータ構造の互換も保てます
- 併せて pickCityConnectionAfter / pickCityConnectionBefore も未使用なので、同じ扱いで整理

### 案2: 今回は触らない

reliability への読み替えだけ行い、デッドコードのまま据え置く。
実装量は減りますが「在来線側が実質機能不全」という状態は変わりません。

---

## 4. しおり画面の表示 — ここが一番の判断ポイント

§0-3 のとおり、しおりには交通タイムラインが出ていません。3案あります。

### 案A ★推奨: しおりに往復の交通行程を描画し、そこにバッジを付ける

```
【1日目】
 ─────────────────────────────────────────────
  10:00  宇都宮駅 発
         🚄 やまびこ・なすの等（約80分）        [目安]
  11:20  仙台駅 着
         ☕ 乗換・待ち（33分）                  [目安]
  11:53  仙台駅 発
         🚄 はやぶさ（約159分）                 [概算]
  14:32  新函館北斗駅 着
         🚃 はこだてライナー（15分・470円）     [確定]
  14:47  函館駅 着
 ─────────────────────────────────────────────
  15:20  ○○ホテル 到着
         🚕 タクシー等で移動 約15分 ¥2,300      [目安]
```

- 現状の「行きのルート（右の経路図を参照）」プレースホルダを実行程に置き換える
- アップされた乗換案内スクショは今までどおり右側に並ぶ（実ダイヤの裏取りとして機能する）
- バッジが一番必要な場所（交通時刻）に、ちゃんとバッジが載る

### 案B: しおり冒頭に「データの確からしさ」サマリーカードだけ置く

行程行は今のまま。カードに「この行程は 確定2件 / 概算8件 / 目安14件」と凡例を出す。
変更は小さいですが、どの行が概算なのかは分かりません。

### 案C ★最小: 既存の現地移動行（タクシー等）と費用表にだけバッジを付ける

交通行程の描画はしない。1時間程度で終わりますが、
ご指摘の「飛行機側のローカル交通時間」は しおりに出ていないので結局見えません。

---

## 5. バッジのデザイン

```html
<span class="rl-badge rl-verified"   title="JR北海道公式・駅探（2026-08-06確認）">確定</span>
<span class="rl-badge rl-researched" title="斜里バス系列 運行情報（2026-09-13確認）／冬季・夏季の季節限定運行">概算</span>
<span class="rl-badge rl-estimated"  title="アプリによる自動計算（実ダイヤ未確認）">目安</span>
```

```css
.rl-badge { font-size:.72em; padding:1px 6px; border-radius:4px; border:1px solid;
            margin-left:6px; white-space:nowrap; vertical-align:middle; }
.rl-verified   { color:#1b7a3d; border-color:#1b7a3d; background:#eaf7ef; }
.rl-researched { color:#a06a00; border-color:#a06a00; background:#fff6e5; }
.rl-estimated  { color:#666;    border-color:#999;    background:#f2f2f2; }
/* caveat 付きは末尾に ⚠ を出す */
```

- **色だけに頼らず、文字（確定 / 概算 / 目安）と枠線で区別**します。
  直近のコミットで印刷時の色再現（print-color-adjust）を整えた経緯があるので、
  白黒印刷でも枠線と文字で読めることを優先します
- `@media print` でも非表示にせず残す（しおりを紙で持ち歩く前提のため）
- caveat がある項目は、ツールチップに加えて行程下に小さく注記を出す
  （例：「※冬季・夏季の季節限定運行です」）— 印刷時は title 属性が見えないため

### 凡例

しおりのスケジュール見出し直下に1行：

> 🟢 確定＝公式時刻表・公示運賃で確認済 ／ 🟡 概算＝複数ソースで確認した実測の代表値 ／ ⚪ 目安＝アプリの自動計算（実ダイヤ未確認）

---

## 6. 実装順序

1. RELIABILITY 定数・reliability() ヘルパを js/data.js に追加（既存挙動に影響なし）
2. AIRPORT_LOCAL_TRANSIT / AIRPORT_ACCESS テーブル化 ＋ 差分ゼロ検証スクリプト
3. pushEdge(text, reliability) 化 — 新幹線側・飛行機側の各区間にレベルを付与
4. lookupLocalTrain のシグネチャ変更 ＋ isApproximateSchedule → reliability（案1採用時）
5. estimateMovement / estimateTaxiFare / 費用表に ESTIMATED を付与
6. CSS バッジ ＋ 凡例 ＋ renderTimeline / 比較画面 renderTimeline への描画
7. しおりへの交通行程描画（案A採用時）
8. 印刷プレビューで白黒でもバッジが読めるか確認

---

## 7. ご判断いただきたい3点（決定済み）

| # | 論点 | 選択肢 | 推奨 | 決定 |
|---|---|---|---|---|
| 1 | 信頼度の段階数 | 3段階（確定 / 概算 / 目安） vs 2段階 | **3段階** | 3段階を採用 |
| 2 | 在来線 lookupLocalTrain | 案1 引数を駅名に変えて復活 / 案2 据え置き | **案1** | 案1を採用。**ただし PREFECTURE_STATIONS への駅名追加は行わない**（個人の出発地が特定されないようにするため） |
| 3 | しおりの表示範囲 | 案A 交通行程を描画してバッジ / 案B サマリーのみ / 案C 現地移動のみ | **案A** | 案Aを採用 |

---

## 8. 実装時の変更点（設計からの差分）

### 8-1. 復路（帰りのルート）を往路の反転で作る仕組みを追加した

案Aで「往復の交通行程を描画する」としたが、実装して初めて分かったのは
**このアプリに復路を生成する手段が存在しない**ことだった。

`generateShinkansenTimeline(stationName, destName, ...)` は「出発地 → 目的地」専用で、
引数を入れ替えて `generateShinkansenTimeline('知床', '宇都宮')` と呼ぶと
分岐条件のどれにも当たらず、既定値の255分・新千歳空港といったデタラメな行程を返す。
最初の実装ではこれに気づかず、しおりの「帰りの交通ルート」が
**「宇都宮駅 発 → 知床 着」と往路と同じ向き**になっていた。

対応として `reverseRouteTimeline()` を新設し、往路のタイムラインを分解して反転する方式にした。
所要時間テーブルは往復で同じ値を使うため、これが手元のデータで作れる唯一まともな復路である。

実装上の注意が2点あった。

1. **タイムラインは node/edge が交互に並んでいない。**
   「宇都宮駅 発 → ☕駅での待ち → 🚄やまびこ → 仙台駅 着」のように edge が2つ続く。
   単純に `revNodes[i]` と `revEdges[i]` を対にすると破綻する。
2. **待ち時間は「直後の移動区間」に紐づけないと、反転時に誤った地点に付く。**
   往路で福島空港にあった搭乗待ちが、復路でも福島空港（＝着いてすぐ帰宅する空港）に
   残ってしまう不具合が実際に出た。搭乗待ちは「飛行機に乗る空港」に付くものなので、
   `decomposeRouteTimeline()` で待ちを後続の移動区間に付随させ、区間ごと反転している。

```
修正前の復路: 知床発 → 女満別空港発(?) → フライト → 福島空港着 → 搭乗待ち(!) → 福島空港発 → 宇都宮着
修正後の復路: 知床発 → 女満別空港着 → 搭乗待ち → 女満別空港発 → フライト → 福島空港着 → 宇都宮着
```

反転で作った行程であることは、しおり上に本文として明示する
（「※ この復路は往路の所要時間を逆順に並べて算出した目安です」）。
地点数と移動区間数が噛み合わない想定外の形のときは `null` を返し、行程を捏造しない。

### 8-2. 各移動区間に durationMin を持たせた
復路の反転に所要時間が必要なため、`pushEdge(text, reliability, durationMin)` に拡張した。
警告文など「移動でない行」は `durationMin: null` のままにしてあり、反転時に無視される。

### 8-3. 在来線区間の実際の配線
`findLocalTrainAccess(駅名)` を新設し、`generateShinkansenTimeline()` の冒頭で
「選択された駅が新幹線駅でなければ、新幹線駅までの在来線区間を先頭に挿入する」ようにした。
LOCAL_TRAIN_CONNECTIONS のキー（`'出発駅-新幹線駅'`）だけを根拠にするため、
区間を1件登録すればそのまま機能する。

**公開版の PREFECTURE_STATIONS には新幹線駅しか無いため、このルックアップは常に null を返す。**
これは意図した状態で、個人の出発地が特定されないようにするため駅名は追加していない。
関数単体では動作を確認済み（平日＝確定、土休日＝目安に切り替わることを検証）。

また、キーワード曖昧マッチ用の `DEPARTURE_TO_LOCAL_STATION`（1件のみ登録されていた）は
新方式では不要になったため削除した。

### 8-4. 乗り換え待ちが20分を超える場合の明示
CLAUDE.md の「待ち時間が20分を超える場合は行程画面に待ち時間そのものを明示すること」に従い、
在来線 → 新幹線の乗り換え待ちが20分超のときは待ち時間を独立した行として出すようにした。

### 8-5. 印刷時のバッジ色を守るための優先度調整
`#confirmed-content span { color: #1f2933 !important; }` という既存の印刷用一括上書きは
IDセレクタを含むため、クラスのみの `.rl-badge` より優先度が高く、
3段階の色分けが潰れてしまう。同じ優先度（`#confirmed-content span.rl-badge.rl-verified` 等）で
明示的に上書きしている。白黒印刷でも文字と枠線で判別できる設計は維持。

---

## 9. 検証結果

| 検証 | 結果 |
|---|---|
| テーブル化・信頼度付与の前後で往路タイムラインが変化しないか（3,120パターン） | **差分ゼロ** |
| 全移動区間に信頼度が付いているか（4,640ルート） | **未付与 0件** |
| 復路の反転が成立するか（4,640ルート） | **失敗 0件** |
| しおり画面のJSエラー | **なし** |
| 印刷メディアでのバッジ色（確定=緑 / 概算=茶 / 目安=グレー） | 上書きに負けず**保持** |
| スマホ幅（390px）での横スクロール | **なし** |

区間の信頼度内訳（全ルート合計）：`researched` 2,228件 / `estimated` 16,900件。
公開版は実ダイヤを持たないため大半が「目安」になるが、
これは §0-2 のとおり**実態を正しく表している**。

---

## 10. 残課題（今回は手を付けていない）

- `LOCAL_TRAIN_CONNECTIONS` の実データは**公開版から削除済み**（§11 参照）。
  形式の説明は架空の駅名を使ったコメント内の例に置き換えてある。
- 出典・確認日が無いローカル交通（網走30分・釧路45分・帯広40分・稚内30分・旭川40分）は
  ESTIMATED として登録した。裏取りできれば RESEARCHED に格上げできる。
- 復路は往路の反転のため、往路と復路で利用する交通手段が変わるケース
  （行きは飛行機・帰りは新幹線など）は表現できない。

---

## 11. 個人情報の除去（追加対応）

`LOCAL_TRAIN_CONNECTIONS` の実データを公開版から削除した。

理由：
- 参照していた `DEPARTURE_TO_LOCAL_STATION` は §8-3 で削除済みで、どこからも呼ばれていなかった
- **キー名（`'在来線駅-新幹線駅'`）自体がオーナーの拠点を示す情報**であり、
  公開リポジトリに残すべきではない

対応：

| 対象 | 内容 |
|---|---|
| `js/data.js` の `LOCAL_TRAIN_CONNECTIONS` | 実データを削除し、空オブジェクトにした |
| 同 コメント | データ形式の説明を、架空の駅名（`'サンプル-サンプル新幹線'`）を使った登録例に差し替え |
| 同 周辺コメント3箇所 | 「個人版では◯◯→函館の…」等、地名を含む記述を地名なしの表現に修正 |
| `CLAUDE.md` 交通データについて | 参照実装として挙げていた区間名を削除し、汎用的な記述に変更。あわせて古い記述（関数の所在が `js/app.js` → 実際は `js/data.js`、廃止した `isApproximateSchedule`）も修正 |
| `CLAUDE.md` | 「データ信頼度の扱い」「個人情報の扱い（公開版）」の節を追加 |
| `design_reliability.md` | 本ドキュメント内の地名も同様に除去 |

空のままでも `findLocalTrainAccess()` が null を返し、行程は従来どおり新幹線駅発として組まれる。
削除後も往路タイムライン3,120パターンで差分ゼロを確認済み。
