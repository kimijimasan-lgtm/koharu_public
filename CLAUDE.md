# こはるプロジェクト 開発ルール

## 交通データについて
- 在来線・新幹線等の移動時刻は、必ずJR各社の公式時刻表等の実データを
  調べた上で設定すること。所要時間の数値だけを使って時刻を機械的に
  生成しない（過去に在来線区間の実装で、この点でデータ不整合が
  発生した経緯がある）
- 新しい出発地・エリアの在来線区間を追加する際は、
  pickLocalTrainDeparture() / pickLocalTrainArrival()（js/data.js）と
  同じ「実在する時刻リストから選択する」方式を踏襲すること。
  登録先は LOCAL_TRAIN_CONNECTIONS で、キーは '在来線駅-新幹線駅' の
  駅ペア形式（CITY_STATION_CONNECTIONS と同じ引き方に揃えてある）
- 乗り換えバッファを基準に選定するが、実際の便間隔（30〜40分に1本など、
  ローカル線区間でよくある）によっては大きな待ち時間が発生しうる。
  待ち時間が20分を超える場合は行程画面に待ち時間そのものを明示すること
- 実データが確認できない区間・ダイヤ（土休日ダイヤ等）は、
  無言で仮の値を使わず、必ず信頼度（下記）で区別すること

## データ信頼度（確定／概算／目安）の扱い
- 時刻・所要時間・運賃には必ず reliability（js/data.js）を持たせる。
  3段階の使い分けは以下のとおり:
  - `VERIFIED`（確定）… 公式時刻表・公示運賃を直接確認した値。
    source と verifiedDate を必ず添える
  - `RESEARCHED`（概算）… 複数ソースで一致を確認した実測値の代表値。
    実在するが「その日のその便」ではない
  - `ESTIMATED`（目安）… アプリが機械生成した値（仮想ダイヤ・距離換算・
    固定値）。実ダイヤ未確認
- 出典・確認日を調べずに値を置くときは、正直に ESTIMATED にすること。
  他の値と見分けがつかない状態で混ぜないこと
- 季節限定運行・所要時間の幅など「利用者が行程を組み替える判断材料」は
  caveat に書く。caveat はツールチップだけでなく行程本文にも表示される
  （印刷時は title 属性が読めないため）
- 調査結果をコードのコメントだけに書かないこと。コメントとコードの
  二重管理になり、フラグとして機能しない。必ず構造化データにする

## 個人情報の扱い（公開版）
- 公開リポジトリには、オーナーの居住地・拠点が特定できる情報を置かない。
  具体的には出発地の駅名・実ダイヤ・その駅を含むデータのキー名など
- このため SHINKANSEN_SCHEDULES / SCHEDULE_ROUTES /
  LOCAL_TRAIN_CONNECTIONS は公開版では意図的に空にしてある。
  空でも動作するよう、各ルックアップは null を返して概算計算に
  フォールバックする設計になっている
- データ形式の説明が必要な場合は、実在の個人的な駅名ではなく
  架空の駅名（サンプル駅等）をコメント内の例として使うこと
- PREFECTURE_STATIONS に駅を追加する際も、居住地が推測できる駅を
  加えないこと

## 所要時間・料金データの「起点」確認（全都市共通の原則）

新しい都市のデータを整備するとき、または既存データを使う機能を作るときは、
**必ず最初のステップとして**「この所要時間・料金は本当にこの駅を起点に
計測された値か」を確認すること。駅名のフィールドが一致していても、
実際の計測起点が別の駅になっている場合がある。

- 確認方法：その駅に隣接している地点（駅前のホテル、駅併設の商業施設など）の
  値を見る。徒歩1分の場所が20分などになっていれば、起点が別の駅である
- 実例（函館）：taxiFromCityStation の全項目が新函館北斗駅起点の値だったが、
  dest.station が新函館北斗駅だったため一見整合して見えていた。
  函館駅から徒歩1分のホテルが「23分」、隣接する函館朝市が「22分」に
  なっていたことで発覚した（この22〜23分は新函館北斗→函館駅の所要時間そのもの）
- この原則は函館限定ではない。金沢・京都・箱根・熱海のデータを整備する際も
  同じ確認を行うこと

### 新幹線駅と市内拠点駅が離れている都市の扱い
- `cityStation` を持つ都市では、`taxiFromCityStation` の起点は
  `station`（新幹線駅）ではなく `cityStation`（市内拠点駅）。両駅間は
  `connectionToCityStation` が指す CITY_STATION_CONNECTIONS の連絡列車で結ぶ
- `cityStation` を持たない都市は `station` が起点（従来どおり）。
  この分岐により、新設した構造は既存都市のデータに影響しない
- 両駅が離れている場合、タクシー1本で済ませず実際の交通手段を行程に
  組み込むこと。函館は17.9kmをタクシー（概算約7,000円）で移動する行程に
  なっていたが、実際は はこだてライナー470円・15分で移動する区間だった

## 航空会社名・便数など「断定的な事実」の扱い

- 航空会社名（ANA/JAL等）・就航路線・1日の便数・列車の固有名称など、
  「事実として断定的に読める具体的情報」は、一次資料（公式時刻表・公式サイト等）
  で実際に検証したものだけを記載してよい。検証していないものは、たとえ
  reliability を ESTIMATED にしていても、具体的な航空会社名や便数そのものを
  文中に書かないこと。バッジは目に入らないことがあるが、文中の固有名詞は
  「事実」として読まれてしまうため
- 実例（2026-09-14 発覚）：`generateFlightTimeline()`（js/data.js）が、出発地の
  最寄り空港名だけを見て「福島空港 発 ※ANA 1日1便」のように表示していた。
  これは目的地側の空港（このケースでは女満別空港）との組み合わせを一切見ておらず、
  実際には福島空港-女満別空港間に直行便は存在しないのに、実在する航空会社名と
  具体的な便数を断定的に表示してしまっていた。出発地→目的地の空港ペアごとに
  就航可否を検証できていない以上、航空会社名・便数は書かず
  「※直行便の有無・便数は要確認（乗り継ぎとなる場合があります）」のように
  利用者自身の確認を促す表現にとどめる
- 新幹線の列車名（はやぶさ・やまびこ等）は、対象区間で実際に運行している
  固有名称のみを使ってよい（JRの公表ダイヤに基づく固定の運行系統のため）。
  一方、航空便は路線・機材・ダイヤ改定で就航状況が変わりやすく、出発空港と
  到着空港の組み合わせごとに個別の検証が必要になる点が新幹線と異なる。
  同列に扱って「主要空港だから何かしら飛んでいるはず」と推測で航空会社名を
  書かないこと
- 新しく空港・航空路線に関するデータやコピーを追加する際は、この節を必ず
  参照し、出発空港×到着空港の組み合わせで直行便の実在を確認できない限り、
  具体的な航空会社名・便数を書かないこと

## データの信頼性表示について
- 移動イベントには timeSource（'verified' / 'estimated'）を
  内部データとして持たせ、実データに基づく値か概算値かを
  常に区別できるようにする

## ホテルデータについて
- ホテル名・料金・設備等は、リブランドや休館の可能性があるため
  実装時に最新の公式情報を確認すること
- 休館期間がある場合は closedPeriod フィールド
  （{ start, end } のISO日付形式）で管理し、
  ホテル選定ロジック側で該当期間と旅程が重なる場合は自動除外する
- ホテル外観の画像は、実際に確認できた公式写真のみを使用する。
  雰囲気の近い無関係な代替写真（フリー素材等）は使用しないこと。
  見つからない場合は null のままにし、プレースホルダー表示にする

## UI全般
- 移動区間（交通手段・所要時間）は、必ず「場所イベント」とは別の
  独立した表示要素として時刻の間に挿入すること
- スマホ・PCで表示すべき機能が異なる場合は、CSSメディアクエリ等の
  シンプルな方法で対応し、不要な機能が別デバイスで表示されないように
  すること（例：QRコードでスマホ確認する機能はPC幅でのみ表示）
